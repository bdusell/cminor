int fib( int x )
{
        if(x<2) {
                return 1;
        } else {
                return fib(x-1) + fib(x-2);
        }
}

int main()
{
        int i=1;
        while(i<20) {
                print "fib(", i, ") = ", fib(i), '\n';
                i=i+1;
        }
        return i;
}

